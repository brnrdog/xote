name: PPX binaries

# Builds the @xote.component ppx as a native binary for every supported
# platform. Called from ci.yml (to keep the pipeline from rotting) and from
# release.yml (the binaries are downloaded into ppx/bin/ and shipped in the
# npm tarball; ppx/postinstall.js selects the right one at install time).

on:
  workflow_call: {}

jobs:
  build:
    name: ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-x64
          - os: ubuntu-24.04-arm
            target: linux-arm64
          - os: macos-15-intel
            target: darwin-x64
          - os: macos-latest
            target: darwin-arm64
          - os: windows-latest
            target: win32-x64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up OCaml
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: "4.14"

      # Same invocation as ppx/build.sh, with the target-specific output path.
      - name: Compile ppx binary
        shell: bash
        run: |
          mkdir -p ppx/bin
          opam exec -- ocamlopt -w -a-31 ppx/ppx.ml -o "ppx/bin/ppx-${{ matrix.target }}.exe"

      # The binary must actually *run* on its host platform — `--help` proves
      # the executable loads (right arch, libc, DLLs), which the release
      # workflow's size check cannot. This is the only step that executes the
      # exact artifact that ships.
      - name: Smoke-test binary
        shell: bash
        run: ./ppx/bin/ppx-${{ matrix.target }}.exe --help

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: ppx-${{ matrix.target }}
          path: ppx/bin/ppx-*.exe
          if-no-files-found: error
